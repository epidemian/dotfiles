[user]
	name = Demian Ferreiro
	email = epidemian@gmail.com
[core]
	excludesfile = ~/.gitignore
[push]
	default = simple
[alias]
	st = status -sb
	ci = commit
	co = checkout
	lg = log --graph --pretty=format:'%C(yellow)%h%Creset -%C(bold magenta)%d%Creset %s %C(dim yellow)(%ar) %C(bold blue)[%an]%Creset' --abbrev-commit --date=relative
	serve = !git daemon --reuseaddr --verbose  --base-path=. --export-all ./.git
	clean-merged = !git branch --merged | grep  -v '^*' | xargs -n 1 git branch -d
	aliases = config --get-regexp 'alias.*'

